# Project notes

- Do not run `npx eslint` or `npx vite build` to verify changes after every edit. The user runs lint/build themselves.
- Do not use Playwright (or any browser automation) to screenshot/debug the app to verify changes. It burns too many tokens. The user will check visual changes themselves.
